export interface UpdatePropertyListing {
  address: string;
  link: string;
  minPrice: string;
  maxPrice: string;
  title: string;
}

export interface UpdatePropertyListingResponse {
  success: boolean;
}
